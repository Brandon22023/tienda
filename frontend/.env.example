# Backend origin used by the browser in Vercel/production.
# Keep it empty for Docker, where nginx proxies the relative /api path.
# For Vercel, set the full Render backend origin without a trailing /api.
VITE_API_URL=

# Development-only Vite proxy target. It is not used by the production build.
VITE_DEV_API_PROXY_TARGET=
